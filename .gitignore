.vscode/

# Local-only easter-egg media — supply your own clips (see assets/README.md).
# Kept out of the repo to avoid publishing copyrighted material.
assets/*.gif
assets/*.mp3
assets/*.mp4
assets/*.webm
assets/*.wav
assets/*.png
assets/*.jpg

# Project-local AWS creds — NEVER commit. Scoped to this project, out of ~/.aws.
infra/.aws/
# Env-specific values (state-bucket URL w/ account ID, Pulumi passphrase).
infra/.env.local
# Pulumi build artifacts
infra/node_modules/
infra/bin/