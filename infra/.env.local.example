# Copy to infra/.env.local (gitignored) and fill in your values.
#   cp infra/.env.local.example infra/.env.local
export PULUMI_BACKEND_URL="s3://<your-pulumi-state-bucket>"
export PULUMI_CONFIG_PASSPHRASE=""   # empty = no encrypted secrets in this stack
